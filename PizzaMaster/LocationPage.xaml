<Page
    x:Class="PizzaMaster.LocationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PizzaMaster"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="10 0 0 0">
            <TextBlock x:Name="headerBlock" Text="Adding location" Style="{ThemeResource HeaderTextBlockStyle}"></TextBlock>
            <TextBlock>Location</TextBlock>
            <TextBox x:Name="nameBox"></TextBox>
            <StackPanel Orientation="Horizontal">
                <Button Content="Save" Click="Save_Click" Margin="10"></Button>
                <Button Content="Cancel" Click="Cancel_Click" Margin="10"></Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
